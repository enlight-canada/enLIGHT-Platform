import React from 'react';
import { Router, Route, Switch } from 'react-router-dom';

const App = () => {
  return (
    <div className="ui container">
      App.js -> put react router with components and Header here
    </div>
  );
};

export default App;
